# 화성생활 내비 제품 요구사항

시민이 행정용어 대신 생활상황을 입력하고, 다음 행동과 신청 순서를 확인하는 서비스입니다. 기준 요구사항은 `CODEX_TASK.md`입니다.

- 36개 로컬 서비스, 16개 빠른 메뉴, 29개 센터를 제공한다.
- 출생 상황은 출생신고를 먼저 안내하고 지원 신청을 연결한다.
- 로컬 검색을 즉시 표시하며 외부 API 실패가 기본 기능을 막지 않는다.
- 등록 데이터와 매 요청 외부 결과를 분리하고 연속검색 결과를 격리한다.
- AI는 등록 서비스 ID 선택만 수행하며 금액·서류·기한·자격을 생성하지 않는다.
- 상세에는 대상·처리 순서·신청방법·서류·비용 안내·기관·전화·공식출처·주의사항을 표시한다.
- 상세의 체크리스트와 인쇄는 현재 화면에서만 동작하며 개인정보를 저장하지 않는다.
- 주소 자동확인은 행정동 기준, 가까운 센터는 저장된 좌표의 직선거리 기준으로 구분한다.
- 위치 거부·지도 키 없음에서도 읍·면·동 선택 및 네이버 지도 웹 링크를 제공한다.
- API 입력 검증, 호출 제한, 개인정보 양단 차단, URL 검증과 XSS 방어를 적용한다.
- Windows `실행.bat`, macOS/Linux `실행.command`로 실행한다.
- 390×844, 412×915, 768×1024, 1440×900에서 브라우저 검증한다.
- 10회 이상 독립 백테스트와 20개 이상 서로 다른 품질영역을 검사한다.

선택 기능의 실제 운영 API 키·배포는 별도 운영환경에서 연결한다. 50개 이상 서비스 확대와 운영 전체 인스턴스의 공유 rate limit은 후속 확장 항목이다. 현재 제공 범위와 외부 본문 확인 제한은 README와 품질보고서에 공개한다.
