# 유지보수 인수인계

## 실행 및 구성

`실행.bat` → `local_preview.py` → `http://127.0.0.1:5500/index.html`. 기본 실행은 패키지·API 키 없이 가능하다. 데이터는 36개 서비스와 29개 센터다.

| 파일 | 역할 |
|---|---|
| `index.html`, `style.css`, `css/enhancements.css` | 시민 검색·빠른 메뉴·센터찾기·반응형 |
| `search-core.js` | 결정론 검색·문맥·제외어·개인정보·URL·확인일 검증 |
| `app.js` | 로컬 우선 결과·상세·API fallback·길찾기 |
| `js/centers.js` | 기기 내 거리 계산·센터 표시·주소지 관할 UI |
| `api/guide.py` | 선택적 AI 서비스 ID 분류 |
| `api/public_services.py` | 외부 공공데이터·지역 필터·병합 |
| `api/address.py`, `api/route.py` | 지도 API 주소·행정동·경로 |
| `api/_common.py`, `api/_naver_maps.py`, `api/_centers.py` | API로 노출하지 않는 공용 모듈 |
| `data/*.json` | 출처가 있는 로컬 서비스·센터 위치 |
| `tests/quality_gate.py` | 회귀·브라우저·백테스트·독립검토 통합 |
| `tests/package_release.py` | 게이트 통과 후 ZIP 생성·재해제 검증 |

## 변경 이유

기존 카드·계좌번호 탐지 누락, 넓은 지역 허용, 행정동과 가까운 센터 혼동, 청사 이전 누락, 자연어 문장의 조사·어미 누락, 외부 응답 대기, Linux 전용 브라우저 경로, 반복 리뷰를 수정했다. 실제 HTTP E2E에서는 CSP를 완화하지 않고 테스트 대기식을 함수로 바꿨다.

## 운영 설정

Vercel 루트는 이 디렉터리. 환경변수는 README 참고. SDK는 openai 3.8.0, 개발 브라우저는 Playwright 1.55.0으로 고정한다. 실제 키 호출이나 배포가 통과했다고 보고하지 않는다. Windows Python 3.12.10에서 검증했고 Vercel 대상 버전은 3.13이다.

## 데이터 갱신

1. 해당 공식 서비스 본문과 개정 내용을 확인한다.
2. `source_checked`/`last_verified`와 `review_due`를 함께 갱신한다. 본문을 확인하지 못하면 `reference_only`와 사유를 남긴다.
3. 검색어는 단일 넓은 단어 대신 `intent_groups`의 문맥 조합을 이용한다. `exclude_keywords`는 클라이언트·서버·AI 결과에 공통 적용된다.
4. 센터 좌표는 공식 찾아오시는길의 지도 위치와 대조한다. 가까운 센터를 관할로 사용하지 않는다.
5. 백테스트 정답을 추가하고 전체 품질게이트를 통과시킨다.

## 알려진 운영 제약

- 위치 권한은 브라우저·사용자 설정에 달려 있다.
- 주소 자동확인과 실시간 경로는 네이버 키 및 서비스 가용성에 달려 있다.
- API 호출 제한은 프로세스 단위이며 엄격한 운영 전체 한도는 외부 공유 계층이 필요하다.
- HTTP 접속 성공은 행정정보 전체 검증을 의미하지 않는다. `SOURCE_AUDIT.json`과 품질보고서의 확인 범위를 유지한다.
- 현재 서비스 범위는 요구 최소 30개를 충족하는 36개이며, 모든 복지사업·모든 민원을 다루지는 않는다.
- 의존성, 테스트 도구, 브라우저 캐시와 실제 `.env`는 배포 ZIP에 포함하지 않는다.
